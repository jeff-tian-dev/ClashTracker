# Copy to .env.gh-pages and fill in your HTTPS API domain.
# Required for GitHub Pages deployment (browsers block HTTP from HTTPS pages).
VITE_BASE_PATH=/Analytics-Dashboard/
VITE_API_URL=https://your-domain.duckdns.org
